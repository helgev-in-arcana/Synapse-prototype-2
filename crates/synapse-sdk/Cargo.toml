[package]
name = "synapse-sdk"
version = "0.1.0"
edition = "2021"
description = "プラグイン側 SDK。Node トレイトと synapse_module! マクロで、unsafe/グローバル/negotiate を作者から隠す"

[lib]
name = "synapse_sdk"

[dependencies]
synapse-abi = { path = "../synapse-abi" }
