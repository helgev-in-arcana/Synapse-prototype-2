[package]
name = "synapse-abi"
version = "0.5.0"
edition = "2021"
description = "Synapse plugin C ABI — single source of truth (Rust), C header generated via cbindgen"

[lib]
name = "synapse_abi"
crate-type = ["rlib"]

[build-dependencies]
cbindgen = "0.29"
