[package]
name = "synapse-abi-suite"
version = "0.5.0"
edition = "2021"
description = "Synapse plugin C ABI — スイート層（host->fetch_suite(id) で取得）"

[lib]
name = "synapse_abi_suite"
crate-type = ["rlib"]

[dependencies]
synapse-abi-core = { path = "../synapse-abi-core" }

[build-dependencies]
synapse-abi-buildgen = { path = "../synapse-abi-buildgen" }
